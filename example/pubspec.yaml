name: dfc_llama_example
description: "Demonstrates how to use the dfc_llama plugin."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  cupertino_icons: ^1.0.8
  dfc_llama:
    path: ../
  file_picker: ^10.2.4
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
